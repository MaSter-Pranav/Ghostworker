{
  "name": "ghostworker-core",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch --env-file=.env src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "check": "tsc --noEmit -p tsconfig.json",
    "simulate": "tsx src/simulation-test.ts"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "acorn": "^8.15.0",
    "axios": "^1.8.0",
    "fastify": "^5.6.0",
    "openai": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.8.0"
  }
}
